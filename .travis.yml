---
dist: bionic

language: python

before_install:
  - cd "$(pyenv root)/plugins/python-build/../../" && git pull
  - pyenv install -s
  - pip install -r requirements.txt
install: buildout
script: bin/test

notifications:
  email:
    on_success: never
    on_failure: never
